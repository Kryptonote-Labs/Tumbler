These documents and the embedded illustration were created for the Tumbler playground and are licensed under the repository's MIT license. They contain no customer content.

Regenerate Word and spreadsheet samples from the repository root with
`bun run --cwd apps/docs samples`. For PowerPoint, run
`bun run compatibility:presentations` and
`bun run compatibility:presentation-rendering`.
