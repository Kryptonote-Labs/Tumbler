node_modules/
dist/
coverage/
compatibility-results/
*.tsbuildinfo
.env
.env.*

# Real customer documents must never enter source control.
fixtures/private/

# Local standards/source archive. Recreate with `bun run references:fetch`.
# These files are useful for implementation but are not part of Tumbler's source.
docs/reference/vendor/
